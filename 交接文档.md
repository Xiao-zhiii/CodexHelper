# Codex 小帮手 · 交接文档

> 写给以后接手本项目的 agent / 开发者。先读这份文档再动手，可以少踩很多坑。
> 最后更新：2026-08-30（v1.7.0，历史记录管理 + 日志查看 + 跨机器路径解析）

---

## 0. 代码结构（v1.5.0 起为包结构，别再往单文件堆代码）

```
src/
├── node_codex_installer.py   # 兼容外观：PyInstaller 入口 + 旧脚本/测试的导入路径
├── NodeCodexSetup.spec       # 打包配置（入口未变，无需改）
├── codexhelper/
│   ├── constants.py          # 常量与下载源（版本号 APP_VERSION 在这里）
│   ├── util.py               # 基础工具（进程执行/路径/detect/环境变量读写）
│   ├── winops.py             # Windows 窗口与输入（窗口查找/前台/剪贴板/SendInput）
│   ├── codex_fix.py          # 插件修复后端（fast-patch 技能/Full Access//goal）
│   ├── gpt_fix.py            # ChatGPT 启动修复后端
│   ├── installer.py          # 后台任务编排（队列消息协议见类文档字符串）
│   ├── netenv.py             # 代理检测 + .codex/.env + 环境变量扫描（安全约定见模块头）
│   ├── mirror.py             # 桌面端镜像：版本列表/多通道下载/SHA256/MSIX 安装
│   ├── codexpaths.py         # ★ v1.7.0 跨机器定位 CODEX_HOME + rollout 路径重定位
│   ├── codexhistory.py       # ★ v1.7.0 历史会话：列表/归档恢复/删除/导入
│   ├── codexlogs.py          # ★ v1.7.0 运行日志读取（logs_2.sqlite）
│   ├── theme.py              # （tkinter 时代的令牌/控件，v1.6.0 起退役保留）
│   ├── app.py + tabs/        # （tkinter 界面，v1.6.0 起退役保留作参考，入口已切 webui）
│   ├── launcher.py           # ★ 程序入口：起服务 → 开浏览器 → 空闲退出/单实例
│   └── webui/
│       ├── cfgcenter.py      # ★ 自 F:/codex helper 整体收编的后端（见模块头说明）
│       ├── server.py         # HTTP 路由：页面/快照/任务系统/历史与日志端点
│       └── page.py           # 前端：在收编页面基础上注入品牌与功能标签页
├── test_fix_headless.py          # 旧回归 20 项（通过兼容外观导入，勿改其导入方式）
└── test_new_features_headless.py # v1.5.0 后端测试（netenv/mirror，含轻量真实网络）
```

**v1.6.0 起界面为本地 Web UI**：`launcher.main()` 启动 ThreadingHTTPServer
（首选端口 17653，落在 Windows 保留段时自动换段/动态端口兜底，端口记录在
`%LOCALAPPDATA%\CodexHelper\port.txt` 供单实例唤起），用 Edge/Chrome `--app`
模式打开，浏览器关闭后空闲 45 秒进程自动退出。
**新增网页功能的套路**：后台任务加进 `server.py` 的 `_ACTION_TARGETS`
（复用 Installer 队列协议，前端经 `/api/task` 轮询自动展示日志/进度/结果）；
纯读取类直接加 GET 端点；页面改动全部写在 `page.py` 的注入块里（锚点：
`</style>`、标签按钮行、`<section id="tab-raw">`、`setInterval(...ping)` 行）。
截图验证用无头 Edge：`msedge --headless=old --virtual-time-budget=20000
--timeout=25000 --screenshot=...`（页面异步数据多，必须给虚拟时间预算）。

## 1. 项目速览

**Codex 小帮手**（原 NodeCodexSetup）—— Windows 单文件绿色 exe，
Python 3.13，PyInstaller onefile 打包（约 54 MB，内嵌 Node.js v24.18.0 离线 msi）。
**v1.6.0 起界面为本地 Web UI**（tkinter 已退役保留）。
功能：① 一键安装 Node.js + Codex CLI ② 一键修复 Codex 桌面端插件
③ ChatGPT 启动修复（写 CODEX_CLI_PATH）④ **Codex 桌面端降级/升级**（v1.5.0，
镜像 Wangnov/codex-app-mirror）⑤ **Codex 环境检测**（v1.5.0，.env + 代理/密钥变量扫描）
⑥ **Codex 历史记录管理**（v1.7.0，归档/恢复/删除/从其它 agent 导入）
⑦ **运行日志查看**（v1.7.0，`.codex/logs_2.sqlite`，跨机器可读）。

作者署名 © 小枳ai分享（页脚、About、exe 属性、暗水印 `_wm()`、零宽字符水印），
**水印相关代码（constants.py 的 `_WW_DATA`/`_wm()`/零宽字符注释行）不要删。**

### 关键路径

| 路径 | 说明 |
|---|---|
| `F:\vibe code\src\node_codex_installer.py` | 主源码（单文件，GUI 在 1280 行之后） |
| `F:\vibe code\src\NodeCodexSetup.spec` | PyInstaller 打包配置 |
| `F:\vibe code\src\version_info.txt` | exe 版本资源（版本号改动要同步这里） |
| `F:\vibe code\Codex小帮手.exe` | 部署成品（用户实际使用的文件） |
| `F:\vibe code\使用说明.txt` | 面向用户的使用说明 |
| `F:\vibe code\交接文档.md` | 本文档 |
| `F:\vibe code\NodeCodexSetup\` | git 仓库副本（github.com/Xiao-zhiii/NodeCodexSetup），源码/版本资源/使用说明需同步 |
| `G:\harness 设计\` | 11 个设计类 skill 的原始存放处（已装入 `~/.zcode/skills/`，见 §5） |
| `~/.zcode/SKILLS_INDEX.md` | 本机 67 个 skill 的分类索引（2026-08-30 生成，见 §5） |

---

## 2. 加载动效清单（重要：以后的 agent 从这里面挑！）

**不要自创加载动效。** 需要给界面加"正在处理"反馈时，从下面 7 种成熟模式里挑：
时长未知 → 1（Spinner）；进度可计算 → 2（Progress Bar）或 3（Circular Progress）；
内容加载占位 → 4+5（Skeleton+Shimmer）；点击防重复 → 6（Button Loading）；
页面级首屏 → 7（Page Loader）。一次只给一个任务用一种指示器，动效要克制
（参考 `~/.zcode/skills/hig-patterns/references/loading.md` 与 hig-foundations/motion.md）。

### 七种模式在本程序中的使用状态（v1.4.1）

| # | 模式 | 状态 | 用在哪 / 怎么实现 |
|---|------|------|-------------------|
| 1 | **Spinner**（不确定等待） | ✅ 使用中 | 底部状态栏左侧 16px 环形旋转指示器（`Spinner` 类，`node_codex_installer.py:1307`）。任务忙（`_set_busy(True)`）或环境检测中（`detect_begin` 消息）时 `spinner.start()`，结束 `spinner.stop()` 后整体隐藏。 |
| 2 | **Progress Bar**（进度可计算） | ✅ 使用中 | 底部 ttk 进度条，**仅 determinate 模式**：下载 Node.js 离线包时按 Content-Length 显示真实百分比（`Installer.download_msi` 发 `("progress", frac)` 队列消息；`None` 表示阶段结束清零）。状态文字同步显示 `12.3/27.4MB（45%）`。 |
| 3 | **Circular Progress**（小空间明确进度） | ⏸ 代码就绪、未启用 | `Spinner.set_progress(frac)` 已实现 determinate 填充（0~1），适合小空间展示明确百分比。当前下载进度用全宽进度条更合适，故未启用；以后若有小区域需要展示确定进度，直接调 `set_progress()` 即可，勿新写控件。 |
| 4 | **Skeleton 骨架屏** | ✅ 使用中 | 两页的检测徽章在检测期间显示灰色"检测中…"占位块（`_start_skeleton()`/`_pulse_skeleton()`，色值 `SKELETON_1`/`SKELETON_2` 呼吸脉冲），结果到达（`render_info`/`render_gpt_info` → `_on_detect_done()`）立即停止动画并上色。布局尺寸固定，不会跳动。 |
| 5 | **Shimmer 微光** | ✅ 与 4 合并使用 | tkinter 无渐变遮罩能力，用骨架徽章 420ms 一次的 `#E2E8F0 ↔ #F8FAFC` 呼吸脉冲近似"流动感"。若以后换 Web 前端，应实现为叠加在骨架屏上的流光，而不是独立使用。 |
| 6 | **Button Loading** | ✅ 使用中 | 被点击的按钮切换为"正在安装/修复/检测…"+动态省略号并禁用（`_set_button_loading(btn, text)`/`_animate_button()`/`_restore_buttons()`），其余操作按钮同时置灰，取消按钮启用。事件入口：`start_task(..., btn=来源按钮)`、`on_fix_click`、`on_fix_gpt_click`；"重新检测"由 `detect_begin` 消息驱动。顺带修掉了"重新检测可连点开多线程"的隐患（`_detecting` 标志防重入）。 |
| 7 | **Page Loader**（页面级全局加载） | ❌ 未使用，也不建议加 | 本程序是本地工具，窗口秒开，检测/任务都在后台线程跑且界面始终可交互；全局遮罩只会增加"被卡住"的感觉。仅当未来出现"整页内容需要联网初始化、期间无法操作"的场景才考虑。 |

### 后台线程 → UI 的消息约定（`Installer.q` 队列，`_poll_queue()` 消费）

- `("log", tag, text)` 日志行；`("status", text)` 状态栏；`("tick", n)` 心跳秒数
- `("progress", frac|None)` **进度条百分比**（仅可计算进度的阶段发）
- `("detect_begin",)` 检测开始（骨架脉冲 + Spinner + 检测按钮加载态）
- `("info", dict)` / `("gpt_info", dict)` 检测结果（两页结果集齐后一起发，
  保证骨架动画在渲染前不中断）
- `("done", ok, summary)` / `("fix_manual", None)` / `("gpt_info", …)`

---

## 3. GUI 结构与布局的坑（这次踩过）

- 窗口 `700×max(520, min(680, 屏高-120))`，minsize 640×520。自上而下：
  标题行 → ttk.Notebook 双分页 → 状态/进度条卡 → ③详细日志卡（expand）→ 页脚。
- **pack 顺序 = 空间分配顺序！** `_build_ui()` 里 foot → head → nb（分页容器）
  先 pack（先拿到请求高度），日志卡、状态卡随后（side=bottom 从底边堆叠，
  日志卡 expand 吸收剩余空间）。**顺序错了分页内容（按钮行、说明文字）会被
  裁掉而 Log 看似正常**——v1.4.1 之前就因为顺序不对，窗口 650px 时②卡底部
  被裁掉约 43px（按钮文字只剩上半截）。
- 改动布局后务必在 680px 和 648px（模拟 768p 屏幕：`root.geometry('700x648+50+50')`）
  两档高度各截一次图检查有没有内容被裁；520px minsize 下允许日志卡被压缩。
- "重新检测"按钮放在①卡标题行右侧（省一行高度）；`ttk.Notebook` Tab
  padding `(16, 4)`；主按钮 `pady=7`。这些数值都是压过的，别随意调大。

## 4. 构建 / 测试 / 发布约定

```bash
# 构建（改完 src/node_codex_installer.py 后）
cd "/f/vibe code/src" && python -m PyInstaller NodeCodexSetup.spec --noconfirm --clean
# 产物 src\dist\NodeCodexSetup.exe → 复制部署为 F:\vibe code\Codex小帮手.exe
```

- 版本号三处同步：`APP_VERSION`（源码 29 行附近）+ `src/version_info.txt`
  （filevers/prodvers/FileVersion/ProductVersion）+ `使用说明.txt` 尾部生成日期。
  同步副本到 `NodeCodexSetup/`（py、version_info.txt、使用说明.txt）。
- 测试：① `python -m py_compile node_codex_installer.py`
  ② `python test_fix_headless.py`（20 项无头回归）
  ③ GUI 冒烟用 PrintWindow 截图（`src/debug_capture.py "<标题>" out.bmp`，
  或 TEMP 下的 `nci_state_shots.py` 状态截图桩），**截图后用
  `PostMessageW(hwnd, 0x0010, 0, 0)` 关窗**。
- 发布（需要用户确认后再做）：git 提交推送 main →
  `gh release create vX.Y.Z`；**release 资产必须先复制成 ASCII 名
  `CodexHelper.exe` 再上传（中文名会被 GitHub 剥离）**。

## 5. 环境/工具坑位（续接必读）

- shell 每条命令后 cwd 重置，用绝对路径或 `cd "/f/vibe code/src" && …`。
- **用户在用电脑：禁止 computer-use 抢鼠标**；验证 GUI 一律 PrintWindow 后台截图。
- G:\harness 设计 的 11 个 skill（apple-hig、hig-components-*、hig-foundations、
  hig-patterns、product-design、steve-jobs、web-prototype-taste-soft）已于
  2026-08-30 装入 `C:\Users\Administrator\.zcode\skills\`（用户级，全局生效）；
  设计决策参考其中的 loading/motion/controls 指南。
- **ZCode 客户端只扫描 `~/.zcode/skills` 和 `~/.agents/skills`，
  不扫 `.codebuddy/skills`**——装技能别放错目录（放错不报错，只是永不生效）。
- 本机共 67 个 skill（用户级 50 + 内置插件 17），完整分类索引已固化在
  `~/.zcode/SKILLS_INDEX.md`（分 10 类：设计前端 / 工程架构 / 规划交付 /
  思维压力测试 / 写作 / 文档处理 / 研究 / ZCode 诊断 / 设备自动化 / 其他）。
  做相关任务前先查这个索引，别重复造轮子。
- **PowerShell 坑**：中文路径的文件名会被静默吞掉（如 `Copy-Item '名字 带空格.png'`），
  涉及中文/空格路径一律改用 Python；内联 `python -c` 的引号也会被 shell 破坏，
  复杂脚本写成 .py 文件再执行。
- 不要在 bash 内联 PowerShell（`$env:` 会被 bash 吞掉）；查环境变量用 Python winreg。
- git push 偶发 443 超时，重试即可；bash 输出中文文件名可能乱码。
- 本机已写入用户级 `CODEX_CLI_PATH`（测"未修复"场景需先清除）。

## 6. 设计规范（v1.4.2 设计走查确立，改动界面请遵守）

依据 `~/.zcode/skills/` 里的 hig-foundations（color/typography/layout/writing/motion）、
hig-patterns（loading/feedback/launching）、hig-components-controls 走查后确立：

**设计令牌**（源码 1290 行附近的常量区）：
- 背景 `BG=#F1F5F9`，卡片 `CARD=#FFFFFF`，边框 `BORDER=#E2E8F0`（Tailwind slate 系）
- 正文 `TXT=#0F172A`，次级 `SUB=#64748B`（白底），浅灰底上的次级文字用 `SUB_DARK=#475569`
  （#64748B 在 #F1F5F9 上对比度 4.4:1 不达标）
- 主色（可交互）`PRIMARY=#2563EB`/`PRIMARY_D=#1D4ED8`；状态色：绿=成功、红=错误/危险、
  琥珀=警告，同一颜色不表达第二种含义
- 次级按钮 chip：`CHIP=#F1F5F9`、悬停 `CHIP_HOVER=#E2E8F0`，一律 `relief="flat", bd=0`
  + `activebackground` 做悬停反馈；**不要再用 `relief="groove"`**（Win95 浮雕感）

**字号下限 9pt**（YaHei UI 8pt 中文不可读，HIG 桌面最小 10pt、中文更需余量）；
字阶：标题 15 粗 / 卡片题 10 粗 / 正文与状态 10 / 次级 9 / 日志 Consolas 9。
**文案**：能用更少词就用更少词，按钮用动词开头（writing.md）。

**tab 样式**：clam 主题下 `TNotebook.Tab` 已配为“未选中灰 chip、选中白底+PRIMARY_D
文字+expand 微放大”，别改回默认。

**适用性结论**：`web-prototype-taste-soft` skill（银色画布/圆角/弹簧动效）只适用于
Web 原型，tkinter 无法实现软阴影/大圆角，勿强行套用；深色模式（dark-mode.md）
tkinter 可做但工作量大，列为将来项，做的话色板需整体替换并重测对比度。

## 7. 当前状态与遗留事项

- v1.7.0（2026-08-30）：**新增【历史记录】与【日志】两个标签页**（标签页总数 9 → 11），
  新增三个后端模块 `codexpaths.py` / `codexhistory.py` / `codexlogs.py`。
  详见 §10（数据结构）、§11（跨机器路径）、§12（踩过的坑）。
  顺带修掉 v1.6.1 遗留的 `pollJob(id)` ReferenceError（§12.4）。
  v1.6.0 起的前端设计令牌体系（三层令牌 + 暗色模式 + 无障碍）也在此版完成。
  **已打包部署 `Codex小帮手.exe`（54,254,069 B）+ 已发布 GitHub Release v1.7.0
  （tag v1.7.0，资产 CodexHelper.exe，Latest）。**
- v1.6.1（2026-08-30）：**修复“打开后一直转圈”**——两个致命 bug：
  ① 页面 JS 每次任务完成都会再次触发检测（detect→完成→refreshDetect→detect…
   无限循环），已改为仅非检测任务结束后刷新；
  ② **webview 模式从未调用 server.serve_forever()**（webview.start() 阻塞主线程，
   端口只监听不 accept，页面请求永远挂起）——serve_forever 必须放入后台线程。
  同时：窗口方案升级为 **pywebview（WebView2 原生内嵌，CC Switch 同款观感）**，
  失败自动回退 Edge --app；pywebview 打包需 spec 里 collect_all(webview/clr_loader/
  pythonnet)。已重建部署（ProductVersion 1.6.1.0）。
  **转圈问题的完整根因链（三个叠加 bug，逐个用 evaluate_js 取证定位）**：
  ① JS 无限检测循环（已修）；
  ② webview 模式缺 serve_forever（已修）；
  ③ **__applyJob 渲染函数里引用了不存在的 `id` 变量**（pollJob 重构遗留），
     每次推送渲染都抛 ReferenceError 被吞 → 按钮永远 busy（已修，附带
     renderInstallDetect 的 gptPkg 空引用、setProgress 空引用两处）。
  **修复经验**：改前端 JS 后必须 `node --check` 服务端脚本 + 用 evaluate_js
  读真实窗口 DOM 验证（PrintWindow 对 WebView2 是空图，不能作为依据）。
  任务状态更新已改为 **Python 推送模式**（每秒 evaluate_js 调 window.__applyJob），
  页面轮询仅作浏览器回退——WebView2 里 setTimeout/fetch 轮询会被节流冻结。
  图标三件套：exe 图标（spec icon=codex_helper.ico）、任务栏（随 exe 图标）、
  页面 favicon（/favicon.ico 路由，数据文件 assets 里 codex_helper.ico）。
  ⚠ 两个排查陷阱：**进程名是 "Codex小帮手"**（跟随部署文件名，不是 NodeCodexSetup，
  清理僵尸实例时 Get-Process 名字别写错）；/exe 旁的 `Codex Helper.log` 记录
  启动每一步（服务/浏览器/窗口），排障先看它。
- v1.6.0（2026-08-30）：**界面整体换装为本地 Web UI**（前端设计与配置中心后端
  自 F:/codex helper 收编，见 webui/cfgcenter.py 模块头）；tkinter 全部功能迁移为
  网页标签页；新增配置中心（config.toml / auth.json / .cc-switch / Codex++ /
  系统信息 / 供应商测活 / 一键修复）。注意：CC Switch 与 Codex++ 本身不是
  webui（前者 Tauri 桌面应用、后者注入工具），本工具只读取并展示其配置。
- v1.5.0（2026-08-30）：①单文件重构为 `codexhelper/` 包（入口兼容外观保住旧测试）
  ②新增【桌面端 降级 / 升级】分页 ③新增【Codex 环境检测】分页。
- v1.4.1：加载动效四件套 + 修复分页内容被裁剪 + 重新检测防重入。
- v1.4.2：HIG 设计走查（扁平按钮/标签页、9pt 字号下限、对比度、文案精简）。
  两版均已重新打包并部署 `Codex小帮手.exe`（当前 ProductVersion 1.5.0.0）。
- version_info 的 InternalName 已由 NodeCodexSetup 改为 Codex小帮手。
- v1.4.2 已发布 GitHub Release（tag v1.4.2，资产 CodexHelper.exe，Latest）。
  **v1.5.0 尚未 commit / release，等用户确认。**
- github.com 443 会间歇性被阻断：push 失败多试几次；工具内的下载通道已为此
  做了多通道兜底（ghproxylist 聚合 → ghproxy.net/gh-proxy.com → 直连，全程代理加持）。
- `gh release create` 不要用 --target 短 SHA（报 target_commitish invalid），
  省略该参数默认 main HEAD。
- 《NodeCodex视频文案脚本与制作建议.md》（workspace/default 下）内容停留在旧名 v1.1.0。

## 8. 桌面端降级/升级 的关键事实（v1.5.0 新功能）

- 镜像仓库 `Wangnov/codex-app-mirror`：release tag 形如 `codex-app-26.825.41651`，
  Windows 资产 `OpenAI.Codex_<4段版本>_<arch>__2p2nqsd0c76g0.Msix`（约 800MB/个）
  + `SHA256SUMS-windows.txt`（安装前校验）。
- **ghproxylist.com 的 URL 前缀返回的是它自己的 HTML 聚合页**（不是文件！），
  但页面是服务端渲染、内含为该文件生成的所有真实代理链接
  （fastgit.cc / gh-proxy.com / ghfast.top / git.yylx.win 等）——
  所以 `mirror.discover_links()` 先抓页解析链接再下载；所有通道都有
  HTML 内容嗅探（Content-Type/首字节），防止把网页当文件存下来。
- 版本列表：GitHub API 直连（per_page=50）→ 经系统代理重试 → **releases.atom 兜底**
  （atom 只有 tag/日期且条数有限，MSIX 文件名与 SHA256 在安装时从 SHA256SUMS-windows.txt 解析）。
- **Windows 保留端口段坑**：`netsh interface ipv4 show excludedportrange protocol=tcp`
  可查；本机 8493–8592 被排除，8517 起整段 bind 失败——launcher 已做
  固定段(17653)+动态端口兜底，改端口前先查排除段。
- **降级流程（用户实测后修订）**：`Add-AppxPackage -ForceUpdateFromAnyVersion`
  对已装更高版本不可靠，降级时必须 `Get-AppxPackage OpenAI.Codex | Remove-AppxPackage`
  先卸载再安装（`mirror.needs_uninstall()` 判定）。卸载只移除应用包与本地缓存，
  **~/.codex 用户数据不受影响**（应用内登录状态会丢，日志中已明示）。
- 代理检测：注册表 WinINET（ProxyEnable/ProxyServer，注意 `http=…;https=…` 格式）
  + HTTP_PROXY/HTTPS_PROXY/ALL_PROXY 环境变量；检测到代理则所有通道走代理。
- 降级安装 = `Add-AppxPackage -ForceUpdateFromAnyVersion`；安装前必须
  taskkill ChatGPT.exe；MSIX 安装不可硬中断（软取消语义，与 msiexec 一致）。
- 版本对比用 4 段 tuple；**tag 里的 build 号（26.825.41651）与 MSIX 版本
  （26.825.5331.0）不是一套数字**，对比一律用 MSIX 文件名里的版本。

## 9. Codex 环境检测 的安全约定（v1.5.0 新功能）

- 扫描范围：`~/.codex/.env` 文件 + 注册表用户变量(HKCU\Environment) +
  系统变量(HKLM\...Session Manager\Environment) + 当前进程环境。
- 命中规则：变量名含 proxy/codex/openai/anthropic，或名/值含
  api_key/apikey/token/secret/password/credential/sk- 等。
- **展示约定：密钥类值只显示首 6 尾 4（打码）；代理地址与普通路径明文，
  超长非敏感值截断到 120 字符。** 本工具面向公开分发，界面截图不能泄露完整密钥。

---

## 10. Codex 数据结构实测（v1.7.0，Codex Desktop 0.142.5）

**这是写历史 / 日志功能的第一手资料，改代码前先对照。**

```
<codex_home>/                          # 默认 %USERPROFILE%\.codex
├── sessions/YYYY/MM/DD/rollout-<ts>-<uuid>.jsonl   # 会话正文（rollout）
├── archived_sessions/…                              # 归档后的 rollout
├── session_index.jsonl                              # {id, thread_name, updated_at}
├── state_5.sqlite        ← threads 表：历史记录的权威索引
├── thread_history_1.sqlite                          # thread_items / thread_turns 投影
├── logs_2.sqlite         ← logs 表：运行日志（本机 170MB / 1.2 万行）
├── sqlite/codex-dev.db                              # 桌面端自有表（⚠ 没有 threads！）
├── config.toml / auth.json
└── backups_state/provider-sync/                     # codex-provider-sync 的备份
```

### `state_5.sqlite` 的 `threads` 表（60 行实测）

列：`id, rollout_path, created_at, updated_at, source, model_provider, cwd, title,
tokens_used, has_user_event, archived, archived_at, cli_version,
first_user_message, preview, is_pinned, thread_source, recency_at, …`

- **归档 = `archived=1` 标记 + rollout 移入 `archived_sessions/`**（不是删记录）。
- **存档路径 `rollout_path` 是绝对路径**，且实测两种写法混用：
  `C:\Users\…` 和 `\\?\C:\Users\…`（Win32 长路径前缀）——见 §11。

### 标题回退链（实测 60 条里 title 只有 52 条非空，CLI 来源的常为空串）

```
title → name → preview → first_user_message → session_index.thread_name → "(无标题)"
```

`preview` 非空率最高（56/60），所以放在 `first_user_message` 之前。

### `logs_2.sqlite` 的 `logs` 表

列：`id, ts, ts_nanos, level, target, feedback_log_body, module_path, file, line,
thread_id, process_uuid, estimated_bytes`

- `feedback_log_body` 是日志正文，**单行很大**（170MB / 1.2 万行）。
- 级别实测分布：`INFO 7048 / TRACE 4101 / DEBUG 928 / WARN 542 / ERROR 16`。
- `ts` 无索引 → 按时间过滤会全表扫；`id` 是主键，按 id 倒序取最快。

### Claude Code 会话格式（导入源，与 Codex 完全不同）

```json
{"type":"user","message":{"role":"user","content":"…"},"uuid":"…","timestamp":"…","cwd":"…","sessionId":"…"}
{"type":"assistant","message":{"role":"assistant","content":[{"type":"text","text":"…"}]},…}
```

转换映射：首行写 `session_meta`；`user` → `event_msg(user_message)` +
`response_item(message/user)`；`assistant` → `response_item(message/assistant)`。
详见 `codexhistory._write_rollout()`。

---

## 11. 跨机器路径解析（`codexpaths.py`）——为什么必须这么做

**问题根源**：`threads.rollout_path` 存的是**绝对路径**，里面写死了旧机器的
用户名和盘符。换电脑 / 换 Windows 账户 / 主目录重定向到 D 盘之后，
文件明明在磁盘上，按这个路径就是读不到。

**解决**：`relocate_rollout()` 只保留 `sessions/`（或 `archived_sessions/`）之后的
相对部分，再拼到**当前**机器的 CODEX_HOME 上：

```
旧：C:\Users\张三\.codex\sessions\2026\08\29\rollout-x.jsonl
新：D:\Users\李四\.codex\sessions\2026\08\29\rollout-x.jsonl
```

CODEX_HOME 解析优先级（参考 codex-provider-sync 的 SQLite Home 顺序）：

1. `CODEX_HOME` 环境变量
2. `~/.codex/config.toml` 根级 `sqlite_home` 的父目录
3. `CODEX_SQLITE_HOME` 环境变量的父目录
4. `%USERPROFILE%\.codex`（默认布局）
5. 注册表 ProfileList 解析真实主目录（应对重定向）
6. MSIX 沙箱 `%LOCALAPPDATA%\Packages\OpenAI.Codex_*\LocalCache\…\.codex`（兜底）

每个候选都要通过 `_valid()`（存在 `sessions` 或 `state_5.sqlite` 之一）。

---

## 12. v1.7.0 踩过的坑（改这几个模块前必读）

### 12.1 ⚠ 相对路径带前导反斜杠 → 文件被移到 C 盘根目录

```python
# 错误：rfind 后 +1 会留下前导反斜杠
rel = norm[i + len(marker) + 1:]        # "\2026\08\29\x.jsonl"
dst = Path(dst_dir) / rel               # → C:\2026\08\29\x.jsonl ！！
```

Windows 上 `Path("D:\\x\\archived_sessions") / "\\2026\\..."` 会被当成**从盘符根
起的绝对路径**，文件被静默移走。正确做法见 `codexhistory._move_rollout()`：
用 `codexpaths.rollout_display()` 拿 `sessions\2026\...`，再切掉首段。
（本次已在 C 盘留下过 `C:\2026` 垃圾，已清理；代码里加了兜底断言防回归。）

### 12.2 ⚠ `threads` 表在 `state_5.sqlite`，不在 `sqlite/codex-dev.db`

新版目录 `sqlite/` 里只有 `local_thread_catalog` 等桌面端自有表。
**不要按"新版目录优先"想当然排序**，`state_db()` 必须把 `state_5.sqlite` 放前面。

### 12.3 ⚠ 兜底断言不能用 `dst.is_absolute()`

`dst_dir` 本身是绝对路径，所以 `dst` 必然是绝对路径——这个断言会把正常情况
也误杀（表现为"归档成功但文件没动"）。正确判据是 **dst 必须位于 dst_dir 之下**：
`dst_dir.resolve() in dst.resolve().parents`。

### 12.4 ⚠ `__applyJob` 里的 `pollJob(id)`（v1.6.1 遗留，已修）

函数形参是 `job` 不是 `id`，运行时抛 ReferenceError 被外层 try/catch 吞掉，
任务永远停在 busy。已改为 `pollJob(job.id)`，并限定**仅浏览器回退模式**续轮询
（推送模式由 Python 每秒调本函数，再叠一层会重复请求）。

### 12.5 日志级别过滤必须下推到 SQL

只在应用层过滤会漏数据：ERROR 只有 16 条且集中在早期，而查询默认按 id 倒序
从最新开始取候选页，用户翻很多页都看不到错误日志——恰恰是最该被找到的。
`level` 列很短，交给 SQLite 扫。关键词仍留在应用层（正文太大）。

### 12.6 其它

- **破坏性操作前强制备份**到 `backups_state/codexhelper/<时间戳>_<动作>/`
  （与 provider-sync 的 `backups_state/provider-sync/` 同层级，互不干扰）。
  备份失败即中止，不冒险改库。
- **Codex 运行时会锁 SQLite**：写入捕获 `OperationalError`，提示用户先关闭
  Codex 桌面端 / CLI。只读一律走 `mode=ro` URI，避免加锁和产生 -wal/-shm。
- **测试绝不能拿真实 `~/.codex`**：复制少量数据到沙箱目录，再 monkeypatch
  `codexpaths.resolve_codex_home` 指过去。本次 180MB 真实会话因此完好无损。
- Claude 会在会话里注入 `<local-command-caveat>` / `<command-name>` /
  `<local-command-stdout>` 等 XML 块（命令回显、免责声明），**不是用户真实输入**，
  导入时必须剥掉，否则标题会变成一串 Caveat 说明（见 `_CLAUDE_INJECTED`）。
- 导入的会话写 `thread_source='import'`，便于在界面上区分来源。
